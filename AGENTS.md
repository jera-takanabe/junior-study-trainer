# AGENTS.md

## 1. 対象

このファイルは、Junior Study Trainer リポジトリ全体で作業する
AIエージェント向けの共通指示である。

サブディレクトリに別の `AGENTS.md` が存在する場合は、
そのディレクトリ配下では、より具体的な指示を追加で適用する。

## 2. 現在の工程

Junior Study Trainer 完成版再開発は、
要求定義フェーズを完了している。

現時点では、作業環境整備および設計開始準備の段階である。

明示的な指示がない限り、次を開始してはならない。

- 完成版のコード実装
- 技術選定
- フレームワーク選定
- データベース製品選定
- API方式の確定
- プロトタイプから完成版へのコード移植

## 3. 作業の基本原則

- 日本語で作業結果を報告する。
- 一度に一つの作業だけを行う。
- 指定された対象範囲外のファイルを変更しない。
- 指示が曖昧な場合は、推測で広範囲に変更せず確認する。
- 複数の大きな変更を同時に行わない。
- 変更前に対象ファイルと変更目的を確認する。
- 変更後に差分と検証結果を確認する。

## 4. 要求と設計

- 要求を実装都合で削除、統合、弱体化しない。
- 設計判断は関連する要求IDへ追跡可能にする。
- 要求内容と設計判断を混同しない。
- 技術上の都合を、要求そのものとして扱わない。
- 要求間依存関係を参照する場合は、正式CSVを使用する。
- 未決事項を黙って固定しない。
- 代替案と採用理由を記録する。

要求依存関係の正式成果物:

`docs/requirements/full_rebuild_requirement_dependencies_v0_1.csv`

## 5. 正式成果物と作業用成果物

正式成果物は、原則として `docs/` 配下を参照する。

`.local/` 配下は、分析、比較、監査、変換等のための
ローカル作業用成果物であり、正式成果物ではない。

`.local/` については、明示的な指示がない限り次を守る。

- 正式な設計根拠として使用しない。
- 通常の参照元にしない。
- Git管理対象に追加しない。
- `docs/` と矛盾する場合は `docs/` を正とする。

教材原本、教科書、ワーク、配布プリント、過去問等の
非公開資料を公開リポジトリへ追加しない。

## 6. プロトタイプの扱い

現行プロトタイプは、完成版へ継続改修しない。

`app/`、既存の `data/`、旧保存形式、旧JSON、
旧ストレージキー等は、操作感や過去の判断を確認する
参考資料として扱う。

明示的な設計判断なしに、プロトタイプの次の要素を
完成版の前提として採用してはならない。

- ディレクトリ構造
- データ構造
- 画面構成
- 保存形式
- ID体系
- アーキテクチャ
- 使用技術

旧データ互換および既存履歴・進捗の移行は行わない。

## 7. Git操作

明示的な指示がない限り、次を行ってはならない。

- `git add`
- `git commit`
- `git push`
- ブランチの作成、削除、切替
- タグの作成、削除
- 履歴を書き換える操作
- 作業ツリーの変更を破棄する操作

Git状態や差分の読み取り確認は行ってよい。

## 8. コマンド実行

- 破壊的なコマンドを実行しない。
- ファイル削除、移動、一括置換は、明示的な許可を得る。
- コマンド実行前に目的と影響範囲を確認する。
- 検証目的の読み取り専用コマンドを優先する。
- 実行結果にエラーがある場合は、隠さず報告する。

## 9. 作業完了時の報告

作業後は、少なくとも次を報告する。

- 実施した作業
- 変更したファイル
- 変更していないこと
- 実行した検証
- 検証結果
- 未解決事項または判断が必要な事項
- Git操作を行っていないこと

変更がない場合も、その旨を明記する。

## 10. 最初に参照する文書

設計開始準備または要求定義の引き継ぎを確認する場合は、
次を優先して参照する。

1. `docs/roadmap/requirements_to_design_handoff_v0_1.md`
2. `docs/requirements/full_rebuild_requirements_phase_completion_v0_1.md`
3. `docs/requirements/full_rebuild_system_overview_v0_1.md`
4. `docs/requirements/full_rebuild_requirement_dependencies_completion_v0_1.md`

## 11. WorkとCodexの開始時点の役割分担

### PC版ChatGPT Work

- Junior Study Trainer完成版再開発の主作業場所とする。
- 要求定義成果物の参照、工程管理、方針検討、設計判断、
  要求IDとの対応確認を担当する。
- 次に行う一つの作業を決め、必要に応じてCodex向け指示を作成する。
- Codexの調査結果、変更内容、差分、検証結果をレビューする。
- 完成版コードの広範囲な直接編集は、開始時点の主担当としない。
- コミットやpushを自動的に行わない。

### VS CodeのCodex

- ローカルリポジトリの調査、限定されたファイル編集、
  検証コマンド実行、差分説明を担当する。
- Workまたはユーザーが指定した一つの作業だけを行う。
- 指定範囲外を変更しない。
- 設計方針、要求の削除・統合、技術選定を独断で確定しない。
- `git add`、`git commit`、`git push`、ブランチ操作を行わない。

### ユーザー

- 重要な設計判断と作業開始を承認する。
- 差分と検証結果を確認する。
- コミットとpushを明示的に実行する。
- WorkとCodexの結果を相互に受け渡す。

### 基本フロー

Workで次の一作業を決定
→ Codex向け指示を作成
→ ユーザーがCodexへ指示
→ Codexが調査・編集・検証
→ ユーザーが結果をWorkへ渡す
→ Workがレビュー
→ ユーザーが必要に応じてcommit・push

この役割分担は開始時点の運用ルールであり、
試行結果や工程の進展に応じて明示的に見直すことができる。
