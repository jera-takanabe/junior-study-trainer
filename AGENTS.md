# AGENTS.md

## 1. 対象

このファイルは、Junior Study Trainer リポジトリ全体で作業する
AIエージェント向けの共通指示である。

サブディレクトリに別の `AGENTS.md` が存在する場合は、
そのディレクトリ配下では、より具体的な指示を追加で適用する。

## 2. 現在の工程

Junior Study Trainer 完成版再開発は、
要求定義フェーズを完了している。

現時点では、作業環境整備および設計開始準備の段階である。

明示的な指示がない限り、次を開始してはならない。

- 完成版のコード実装
- 技術選定
- フレームワーク選定
- データベース製品選定
- API方式の確定
- プロトタイプから完成版へのコード移植

## 3. 作業の基本原則

- 日本語で作業結果を報告する。
- 一度に一つの作業だけを行う。
- 指定された対象範囲外のファイルを変更しない。
- 指示が曖昧な場合は、推測で広範囲に変更せず確認する。
- 複数の大きな変更を同時に行わない。
- 変更前に対象ファイルと変更目的を確認する。
- 変更後に差分と検証結果を確認する。

## 4. 要求と設計

- 要求を実装都合で削除、統合、弱体化しない。
- 設計判断は関連する要求IDへ追跡可能にする。
- 要求内容と設計判断を混同しない。
- 技術上の都合を、要求そのものとして扱わない。
- 要求間依存関係を参照する場合は、正式CSVを使用する。
- 未決事項を黙って固定しない。
- 代替案と採用理由を記録する。

要求依存関係の正式成果物:

`docs/requirements/full_rebuild_requirement_dependencies_v0_1.csv`

## 5. 正式成果物と作業用成果物

正式成果物は、原則として `docs/` 配下を参照する。

`.local/` 配下は、分析、比較、監査、変換等のための
ローカル作業用成果物であり、正式成果物ではない。

`.local/` については、明示的な指示がない限り次を守る。

- 正式な設計根拠として使用しない。
- 通常の参照元にしない。
- Git管理対象に追加しない。
- `docs/` と矛盾する場合は `docs/` を正とする。

教材原本、教科書、ワーク、配布プリント、過去問等の
非公開資料を公開リポジトリへ追加しない。

## 6. プロトタイプの扱い

現行プロトタイプは、完成版へ継続改修しない。

`app/`、既存の `data/`、旧保存形式、旧JSON、
旧ストレージキー等は、操作感や過去の判断を確認する
参考資料として扱う。

明示的な設計判断なしに、プロトタイプの次の要素を
完成版の前提として採用してはならない。

- ディレクトリ構造
- データ構造
- 画面構成
- 保存形式
- ID体系
- アーキテクチャ
- 使用技術

旧データ互換および既存履歴・進捗の移行は行わない。

## 7. Git操作

### 7.1 新運用の開始条件

今回の `AGENTS.md` 更新だけでは、
条件付きGit反映運用を開始しない。

`docs/roadmap/work_codex_handoff_protocol_v0_1.md` が
別作業で更新され、両文書の整合性が確認されるまでは、
Codexは `git add`、`git commit`、`git push` を行わない。

両文書の整合性確認後、小規模試験から新運用を開始する。
この開始条件は、以下の条件付きGit反映許可より優先する。

### 7.2 Git反映を許可できる条件

新運用の開始後、Codexが `git add`、`git commit`、
`git push` を実行できるのは、次をすべて満たす場合だけとする。

- Workまたはユーザーの指示に「Git反映まで許可」と明記されている。
- 変更対象ファイルが明確である。
- commit対象ファイルが明確である。
- commitメッセージが明確である。
- push先のリモートとブランチが明確である。
- 作業開始前の作業ツリーがクリーンである。
- 変更が指定範囲内だけである。
- ファイルの削除、移動、名称変更を含まない。
- 指定された検証がすべて成功している。
- 警告、未解決事項、判断待ちがない。
- 秘密情報、認証情報、非公開教材を含まない。
- 要求、重要な設計判断、公開API、データベース構造、
  依存関係へ影響しない。
- 明示されたファイルだけをステージする。
- ステージ済み差分が指示内容と一致している。

Git反映の許可が明記されていない場合、
CodexはGit書き込み操作を行わない。
Git状態や差分の読み取り確認は行ってよい。

### 7.3 commitまたはpush前の停止条件

次のいずれかに該当する場合、Codexはcommitまたはpushへ進まず、
Workまたはユーザーへ報告して判断を求める。

- 作業開始前から既存変更または未追跡ファイルがある。
- 現在のブランチが指示と一致しない。
- push先が指示と一致しない。
- ahead／behindや分岐が想定外である。
- 対象外ファイルに変更がある。
- 変更が複数領域へ広がった。
- ファイルの削除、移動、名称変更が必要になった。
- 差分が当初の想定より大きい。
- 要求、設計、公開API、データベース構造、
  依存関係へ影響する。
- 検証が失敗した。
- 警告、未確認事項、未解決事項、判断待ちがある。
- 秘密情報、認証情報、非公開教材を含む可能性がある。
- ステージ内容が指示されたcommit対象と一致しない。
- pushが非fast-forwardなど通常外の操作を要求する。
- 通常条件を満たしていると確認できない。

commit後にpushだけ失敗した場合は、
commit済み・push未完了として報告する。
履歴書き換えや変更破棄による独断解決は行わない。

### 7.4 常に禁止するGit操作

新運用の開始後も、Codexは次を実行してはならない。

- force push
- force-with-lease
- rebase
- commit amend
- resetによる変更またはcommitの破棄
- `git clean`
- checkout、restoreなどによる既存変更の破棄
- ブランチの削除
- タグの作成、変更、削除、push
- 指示されていないブランチへの切替
- ブランチの作成
- マージ
- 競合の独断解決
- 既存変更を含む一括ステージ
- 指示されていないリモートまたはブランチへのpush

これらが必要になった場合は通常作業として実行せず、
停止してユーザーの判断を求める。

## 8. コマンド実行

- 破壊的なコマンドを実行しない。
- ファイル削除、移動、一括置換は、明示的な許可を得る。
- コマンド実行前に目的と影響範囲を確認する。
- 検証目的の読み取り専用コマンドを優先する。
- 実行結果にエラーがある場合は、隠さず報告する。

## 9. 作業完了時の報告

作業後は、少なくとも次を報告する。

- 実施した作業
- 変更したファイル
- 変更していないこと
- 実行した検証
- 検証結果
- 未解決事項または判断が必要な事項
- 作業開始前のGit状態

変更がない場合も、その旨を明記する。

Git反映を行った場合は、さらに次を報告する。

- ステージしたファイル
- ステージ済み差分の確認結果
- commit ID
- commitメッセージ
- push先
- push結果
- push後のGit状態
- 停止した場合は、commit前、commit後、push前、
  push後のどの段階か
- 常に禁止されるGit操作を行っていないこと

Git反映を行わなかった場合は、
その理由とGit書き込み操作を行っていないことを報告する。

## 10. 最初に参照する文書

設計開始準備または要求定義の引き継ぎを確認する場合は、
次を優先して参照する。

1. `docs/roadmap/requirements_to_design_handoff_v0_1.md`
2. `docs/requirements/full_rebuild_requirements_phase_completion_v0_1.md`
3. `docs/requirements/full_rebuild_system_overview_v0_1.md`
4. `docs/requirements/full_rebuild_requirement_dependencies_completion_v0_1.md`

## 11. WorkとCodexの開始時点の役割分担

### PC版ChatGPT Work

- Junior Study Trainer完成版再開発の主作業場所とする。
- 次に行う一作業を決定する。
- Codex向け指示を作成する。
- Git反映の可否、commit対象、commitメッセージ、
  push先、停止条件を明示する。
- 要求定義成果物を参照し、工程管理、方針検討、
  重要な要求・設計判断、要求IDとの対応確認を行う。
- Codexの完了報告を事後レビューする。
- 例外発生時に、ユーザー承認が必要かを判断する。
- 完成版コードの広範囲な直接編集は、開始時点の主担当としない。
- Gitコマンドを自動実行しない。

### VS CodeのCodex

- 作業開始前のGit状態を確認する。
- 指定された一作業だけを調査・編集する。
- 指定された検証を実行する。
- 差分を自己確認する。
- Git反映条件を判定する。
- 明示的に許可され、すべての通常条件を満たす場合だけ、
  指定ファイルの `git add`、commit、
  指定先への通常pushを実行する。
- ステージ済み差分を確認する。
- push後のGit状態を確認する。
- Git反映結果を含む完了報告を行う。
- 停止条件に該当した場合は、
  commitまたはpushへ進まず報告する。
- 設計方針、要求の削除・統合、技術選定を独断で確定しない。

### ユーザー

- 重要な設計判断を承認する。
- 大規模、破壊的、例外的な操作を承認する。
- 運用ルールの変更を承認する。
- 通常条件を満たす作業では、Gitコマンドを実行しない。
- 例外時に必要な最終判断を行う。

### 基本フロー

1. Workが次の一作業を決定する。
2. WorkがGit反映の可否と条件を含むCodex向け指示を作成する。
3. ユーザーが必要な重要判断を承認し、Codexへ指示を渡す。
4. Codexが作業開始前のGit状態を確認する。
5. Codexが指定範囲内で調査・編集する。
6. Codexが検証する。
7. Codexが差分を自己確認する。
8. CodexがGit反映条件を再判定する。
9. 条件を満たす場合、
   Codexが明示されたファイルだけを `git add` する。
10. Codexがステージ済み差分を確認する。
11. Codexが指定されたメッセージでcommitする。
12. Codexが指定されたリモート・ブランチへ通常のpushを行う。
13. Codexがpush後のGit状態を確認する。
14. CodexがWorkへ完了報告を返す。
15. Workが事後レビューする。
16. 問題がある場合は、履歴を書き換えず、
    別の一作業・別のcommitで修正する。

停止条件に該当した場合はGit反映へ進まず、
Workまたはユーザーへ判断を求める。

第7.1節の開始条件を満たすまでは、
この基本フロー中の `git add`、commit、pushを行わない。

この役割分担は開始時点の運用ルールであり、
試行結果や工程の進展に応じて明示的に見直すことができる。
